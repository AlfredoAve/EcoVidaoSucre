# Copia este archivo a .env y completa los valores reales
# cp .env.example .env

# Puerto del servidor (por defecto 3001)
PORT=3001

# Secret para firmar los tokens JWT — cambia esto por una cadena larga y aleatoria
JWT_SECRET=cambia_esto_por_un_secret_seguro_y_largo
